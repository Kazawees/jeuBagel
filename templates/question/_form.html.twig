{{ form_start(form) }}
<div style="width:60%;" class="text-center mx-auto">
    {{ form_widget(form) }}
</div>
    <button class="mt-2 original text-center">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
