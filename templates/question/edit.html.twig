{% extends 'base.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
<div class="container text-center">
    <h2>Modifier question</h2>
    <div class="row text-center">
        <div class="text-center mx-auto" style="width:60%">
            {{ include('question/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
            <br>
        <div class="mt-4">
            <button type="button"class="turbo"><a class=" text-dark text-decoration-none " href="{{ path('app_question_index') }}">Retourner à la liste</a></button>
        </div>
        
        {{ include('question/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
